project_id = "omni-commerce"

# Locked ports — keep in sync with infra/config/local-ports.json (avoid 54321+ defaults used by other projects).
[api]
port = 54721

[db]
port = 54722

[studio]
port = 54723

[inbucket]
port = 54724

[analytics]
port = 54727

[db.seed]
enabled = true
sql_paths = ["./seed/dev.sql"]
